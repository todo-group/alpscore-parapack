include_directories(include)
SET(TESTS expression complex)
foreach(name ${TESTS})
  add_executable(${name} test/${name}.cpp)
  target_link_libraries(${name} ${ALPSCore_LIBRARIES} gtest_main)
  add_test(NAME ${name} COMMAND ${name})
endforeach(name)  
